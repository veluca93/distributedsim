#ifndef DISTSIM_COMMON_HPP
#define DISTSIM_COMMON_HPP
#include <cstddef>

template <typename T>
class Node;

template <typename T>
class Message;

template <typename T>
class HardwareManager;

typedef std::size_t node_id_t;

#endif
